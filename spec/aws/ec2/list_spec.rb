require "spec_helper"

describe Aws::Ec2::List do
  it "has a version number" do
    expect(Aws::Ec2::List::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
